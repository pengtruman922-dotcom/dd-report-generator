"""Prompt for v4 index builder."""

INDEX_BUILDER_PROMPT = """你负责为 v4 的 info_chunk 生成检索摘要和索引标签。

## 目标

- `info_summary`：用于列表和推送的简洁摘要
- `info_index_tags`：用于检索的标签
- `tracking_summary`：仅供系统内查看，不用于外部推送

## 规则

- 只围绕 info_chunk 生成对外检索标签
- 不把买家态度、我方策略、推荐建议做成标签
- 标签尽量短、稳定、可复用，避免整句
- 标签应更偏检索词：主体别名、行业/赛道、核心产品、地域、上市/非上市状态、交易状态、关键资质、风险类型
- 不要只生成泛词；优先生成用户可能搜索的名词短语，例如“数据中心节能”“光栅编码器”“番茄深加工”“港股IPO”“专精特新”
- 摘要聚焦主体、赛道、核心业务、关键财务/交易事实

## 输出格式

```json
{
  "info_summary": "简洁摘要",
  "info_index_tags": ["标签1", "标签2"],
  "tracking_summary": "系统内动态摘要"
}
```
"""
